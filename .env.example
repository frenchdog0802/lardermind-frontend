# Leave empty for local Vite: /api and /auth proxy to backend-cf at 127.0.0.1:8787
VITE_API_BASE_URL=
VITE_GOOGLE_CLIENT_ID=

# Remote / Pages build examples (no Vite proxy):
# VITE_API_BASE_URL=https://api.lardermind.com
# VITE_GOOGLE_CLIENT_ID=<Web OAuth Client ID — same as Worker GOOGLE_CLIENT_ID>
